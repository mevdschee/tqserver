# Metrics

*(Documentation In Progress)*

Future versions of TQServer may expose a `/metrics` endpoint for Prometheus scraping, including:
-   Requests per second (global/worker)
-   Latency histograms
-   Worker restart counts
-   Active worker count
